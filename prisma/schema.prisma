generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Artist {
  id          Int      @id @default(autoincrement())
  name        String
  category    String
  location    String   @default("Mumbai")
  bio         String   @default("")
  price       String   @default("On Request")
  imageUrl    String   @default("/images/placeholder-artist.jpg")
  isExclusive Boolean  @default(false)
  isFeatured  Boolean  @default(false)
  isTrending  Boolean  @default(false)
  isActive    Boolean  @default(true)
  rating      Float    @default(4.5)
  eventsCount Int      @default(0)
  videoUrl    String   @default("")
  order       Int      @default(0)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model BookingLead {
  id         Int      @id @default(autoincrement())
  name       String
  phone      String
  email      String   @default("")
  eventType  String
  eventDate  String
  budget     String
  message    String   @default("")
  artistId   Int?
  artistName String   @default("")
  status     String   @default("new")
  createdAt  DateTime @default(now())
}

model ContactMessage {
  id        Int      @id @default(autoincrement())
  name      String
  phone     String
  message   String
  createdAt DateTime @default(now())
}

model Testimonial {
  id        Int      @id @default(autoincrement())
  name      String
  role      String   @default("")
  content   String
  rating    Int      @default(5)
  imageUrl  String   @default("")
  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())
}

model NewsletterSubscriber {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  createdAt DateTime @default(now())
}

model TrendingArtist {
  id        Int      @id @default(autoincrement())
  name      String
  imageUrl  String
  order     Int      @default(0)
  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())
}

model ExclusiveArtist {
  id        Int      @id @default(autoincrement())
  name      String
  category  String   @default("Singer")
  location  String   @default("Mumbai")
  bio       String   @default("")
  price     String   @default("On Request")
  imageUrl  String
  rating    Float    @default(5.0)
  order     Int      @default(0)
  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())
}
